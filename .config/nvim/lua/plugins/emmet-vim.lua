return {
  {
    "mattn/emmet-vim",
    config = function()
      -- Optional configuration, you can customize it if needed
      -- Example: remap the trigger key for emmet expansion
--			vim.api.nvim_set_keymap('i', '<C-y>', '<Plug>EmmetExpandAbbr', { noremap = true, silent = true })
    end
  }
}

